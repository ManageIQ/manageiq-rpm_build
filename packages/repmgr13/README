Build with:

Fetch source files from https://rpm-manageiq-org.nyc3.digitaloceanspaces.com/index.html?prefix=sources_cache/repmgr13/ if needed.

```bash
mock --spec=./repmgr.spec -r centos-stream-9-x86_64 --sources=./
```
